'use strict';

const { ProxyService } = require('@semapps/crypto');

/**
 * HTTP signature proxy for federated requests. Re-exported as-is from
 * @semapps/crypto — no Kind-specific config needed.
 */
module.exports = ProxyService;
